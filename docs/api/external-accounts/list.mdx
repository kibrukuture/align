---
title: "List External Accounts"
description: "Retrieve all linked bank accounts for a customer"
icon: "list"
---

## Method Signature

```typescript
align.externalAccounts.list(
  customerId: string
): Promise<ExternalAccountListResponse>
```

## Example

```typescript
const response = await align.externalAccounts.list(customerId);

for (const account of response.items) {
  console.log(`${account.id}: ${account.bank_name} (${account.account_type})`);
}
```

## Related Methods

<CardGroup cols={2}>
  <Card
    title="Create Account"
    icon="building"
    href="/docs/api/external-accounts/create"
  >
    Link a new bank account
  </Card>
</CardGroup>
